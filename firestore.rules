// Firestore security rules.
// Server (Admin SDK) bypasses these. Lock down direct client access until
// you add Firebase Auth on the frontend.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
